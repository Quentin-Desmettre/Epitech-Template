##
## EPITECH PROJECT, 2022
## Epitech-Template
## File description:
## workflow.yml
##

name: "Push to remote"
on:
  push:
    branches:
      - 'dev/**'
jobs:
  deploy:
    name: Deploy to Epitech repo
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${GITHUB_REF#refs/heads/}

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_KEY }}
          known_hosts: ${{ secrets.KNOWN_HOSTS }}
          if_key_exists: replace

      - name: Push to Epitech
        run: "git remote add epiRepo git@github.com:Quentin-Desmettre/criterion.git && git push epiRepo ${GITHUB_REF#refs/heads/}"
